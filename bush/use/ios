# Copyright (C) 2010, Guy Barrand. All rights reserved.
# See the file bush.license for terms.

if [ "${use_ios}" = "yes" ] ; then

  libs="${libs} -framework MessageUI"
  libs="${libs} -framework UIKit"
  libs="${libs} -framework Foundation" # NS functions.
  libs="${libs} -framework QuartzCore" # CA functions.
  libs="${libs} -framework CoreMotion" # Accelerometer, Gyroscope, Magnetometer.

fi
